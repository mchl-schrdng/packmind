node_modules/
dist/
coverage/
*.log
.DS_Store
*.tmp
*.tmp.*

# PackMind dogfooding its own repo — not part of the source tree
# (CLAUDE.md itself IS committed — it's the project's hand-written dev guide)
.packmind/
.mcp.json
.claude/

# PackMind per-developer state (not committed in consuming projects):
# .packmind/journal.md
# .packmind/usage.json
# .packmind/state/
# .packmind/recall/
# .packmind/hooks/*.lock

# packmind dogfooding backups
*.packmind-bak
