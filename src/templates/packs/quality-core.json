{
  "name": "quality-core",
  "version": 1,
  "rules": [
    {
      "id": "no-console-log",
      "message": "console.log left in source - remove it or use a real logger before finishing.",
      "severity": "warn",
      "pathGlob": "src/**",
      "content": "console\\.log"
    }
  ],
  "checks": [
    {
      "id": "src-without-tests",
      "message": "src/** changed but no test was written this session. Add or adjust a test, or call record_evidence({check:\"tests-updated\", detail:\"why not\"}).",
      "changedGlobs": ["src/**"],
      "missingChangedGlobs": ["test/**", "tests/**", "**/*.test.*", "**/*.spec.*"],
      "needsEvidence": "tests-updated"
    }
  ]
}
