{
  "name": "github-actions",
  "version": 1,
  "rules": [
    {
      "id": "pin-actions-to-sha",
      "message": "Pin third-party GitHub Actions to a full commit SHA (not a tag) to prevent supply-chain tampering.",
      "severity": "warn",
      "pathGlob": ".github/workflows/**",
      "content": "uses:\\s+[^@\\n]+@(?![0-9a-f]{40})"
    }
  ],
  "checks": [
    {
      "id": "workflow-review",
      "message": "A GitHub Actions workflow changed this session. Check permissions scope, secret usage, trigger events, and action pinning, then record_evidence({check:\"workflow-reviewed\"}).",
      "changedGlobs": [".github/workflows/**"],
      "needsEvidence": "workflow-reviewed"
    }
  ]
}
